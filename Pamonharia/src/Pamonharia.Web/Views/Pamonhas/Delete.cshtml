@model Pamonharia.Application.ViewModels.PamonhaViewModel

<h2>Excluir Pamonha</h2>
<h3>Tem certeza que deseja excluir?</h3>
<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">Sabor</dt>
        <dd class="col-sm-10">@Model.Sabor</dd>
        
        <dt class="col-sm-2">Categoria</dt>
        <dd class="col-sm-10">@Model.CategoriaNome</dd>

        <dt class="col-sm-2">Preço</dt>
        <dd class="col-sm-10">@Model.Preco.ToString("C")</dd>
    </dl>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <button type="submit" class="btn btn-danger">Excluir</button> |
        <a asp-action="Index">Voltar</a>
    </form>
</div>